[workspace]
members = ["lively-swc-transforms", "lively-swc-plugin", "lively-swc-browser"]
resolver = "2"

[profile.release]
lto = true
opt-level = 3

# Size-optimized profile for browser WASM
[profile.browser-release]
inherits = "release"
opt-level = "z"
strip = "symbols"
codegen-units = 1
